{
  "dependencies": {
    "ethers": "5",
    "fs": "^0.0.1-security",
    "solc": "^0.8.15"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_module/ --base-path . -o . SimpleStorage.sol SimpleStorage.sol"
  }
}
